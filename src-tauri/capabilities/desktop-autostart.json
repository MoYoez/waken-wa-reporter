{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-autostart",
  "description": "Desktop-only autostart capability for the main window",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": ["autostart:default"]
}
